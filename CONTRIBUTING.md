# 贡献指南

欢迎贡献！本仓库由 **@c991china** 维护，并与 **[@22178384](https://github.com/22178384)** 通过互相提 PR、一起写的方式协作。

## 如何贡献
1. Fork 本仓库，从 `main` 切一个 `feat/xxx` 分支。
2. 提交清晰的改动（遵循现有风格，中文注释优先）。
3. 开 PR 到本仓库的 `main`，描述做了什么、为什么。
4. 维护者合并后，你的账号会出现在 contributors 里。

## 约定
- 代码用 4 空格缩进；shell 脚本以 `#!/usr/bin/env bash` 开头并 `set -euo pipefail`。
- 文档用 Markdown，示例要能直接跑。
- 提交信息用中文或英文均可，但要说明意图。
